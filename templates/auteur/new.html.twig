{% extends 'base.html.twig' %}

{% block title %}Nouvel auteur{% endblock %}

{% block body %}
<div class="form-wrapper">
    <a href="{{ path('app_auteur_index') }}" class="detail-back">Retour aux auteurs</a>
    <div class="form-card">
        <h1 class="form-card__title">Créer un auteur</h1>
        {{ include('auteur/_form.html.twig') }}
    </div>
</div>
{% endblock %}
