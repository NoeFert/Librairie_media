{% extends 'base.html.twig' %}

{% block title %}Nouvelle franchise{% endblock %}

{% block body %}
<div class="form-wrapper">
    <a href="{{ path('app_franchise_index') }}" class="detail-back">Retour aux franchises</a>
    <div class="form-card">
        <h1 class="form-card__title">Créer une franchise</h1>
        {{ include('franchise/_form.html.twig') }}
    </div>
</div>
{% endblock %}
